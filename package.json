{
  "name": "render-token-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ czarly/render-token-subgraph",
    "create-local": "graph create --node http://localhost:8020/ czarly/render-token-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ czarly/render-token-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 czarly/render-token-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
